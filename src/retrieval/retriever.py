"""Retrieves relevant chunks for a question, scoped to one or more products."""


def retrieve(question, product_names, top_k=None):
    """Return the top_k chunks most relevant to question, scoped to product_names."""
    raise NotImplementedError
