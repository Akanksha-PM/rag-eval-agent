"""Splits plain text into overlapping chunks for embedding and retrieval."""


def chunk_text(text, chunk_size=None, chunk_overlap=None):
    """Split text into a list of overlapping chunks of roughly chunk_size chars."""
    raise NotImplementedError
